#include <stdio.h>
#include "main.h"
/**
 * mul - check that multiplies two integers.
 * @a: characters to be checked.
 * @b: characters to be checked.
 * Return: result of n.
 */
int mul(int a, int b)
{
int n;
n = a * b;
return (n);
}
